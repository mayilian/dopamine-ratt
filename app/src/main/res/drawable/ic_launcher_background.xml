<?xml version="1.0" encoding="utf-8"?>
<!-- The red of the sign itself, so the mask crops into the placard rather than around it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#D30300" />
</shape>
