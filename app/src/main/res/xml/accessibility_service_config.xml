<?xml version="1.0" encoding="utf-8"?>
<!--
    Two jobs, and they need different things.

    Knowing which app came to the front needs the package name on the event and
    nothing more. Knowing that Instagram has landed on Reels means reading view
    ids out of the window, which needs canRetrieveWindowContent. It is declared
    here because a capability cannot be granted later, but the content events
    that make it usable are only subscribed to while a surface is armed, and the
    service reads view ids only, never the text on them.

    packageNames is deliberately not set: we need events from other apps too,
    otherwise we cannot tell "entered Instagram from elsewhere" apart from
    "already scrolling inside Instagram".
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="150"
    android:description="@string/service_description" />
