<?xml version="1.0" encoding="utf-8"?>
<!--
    We only need to know which app just came to the front, so window content
    retrieval stays off. The service reads event.packageName and nothing else.

    This is not only a privacy line, it is what makes the app installable. A
    sideloaded app whose accessibility service can read window content is the
    shape of a banking trojan, and Play Protect removes it within a minute of
    being installed. Watching for one screen inside an app needs that capability,
    which is why this app does not do it.

    packageNames is deliberately not set: we need events from other apps too,
    otherwise we cannot tell "entered Instagram from elsewhere" apart from
    "already scrolling inside Instagram".
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="150"
    android:description="@string/service_description" />
