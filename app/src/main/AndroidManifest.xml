<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        The first is needed to list installed apps in the picker. The second is
        the home screen, which has to be recognisable as somewhere you went, or
        backing out of a watched app and walking straight back in would look
        like you never left it.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
    </queries>

    <!--
        Backup is off on purpose. The watchlist is a record of which apps you are
        trying to use less, which is not something to sync into a cloud backup or
        restore onto a different device.
    -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Ratt">

        <activity
            android:name=".SetupActivity"
            android:exported="true"
            android:theme="@style/Theme.Ratt">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Its own task affinity so it never merges into the target app's task,
            and it stays out of recents so it does not look like a real screen.
        -->
        <activity
            android:name=".InterstitialActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity=".interstitial"
            android:excludeFromRecents="true"
            android:screenOrientation="portrait"
            android:configChanges="keyboardHidden|navigation|orientation|screenSize|screenLayout|uiMode"
            android:theme="@style/Theme.Ratt.Interstitial" />

        <service
            android:name=".RattAccessibilityService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
